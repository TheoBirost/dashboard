{{ form_start(form, {'attr': {'class': 'space-y-4 bg-white p-6 shadow-lg rounded-lg'}}) }}

    {{ form_widget(form, {'attr': {'class': 'w-full p-3 border border-gray-300 rounded-md focus:ring-2 focus:ring-blue-500'}}) }}

<button type="submit"
        class="px-6 py-2 bg-[#06D6A0] hover:bg-[#05c093] text-white font-bold rounded-full shadow-md transition duration-300">
    💾 {{ button_label|default('Save') }}
</button>

{{ form_end(form) }}
