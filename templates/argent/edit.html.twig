{% extends 'base.html.twig' %}

{% block title %}Modifier Argent{% endblock %}

{% block main %}
    <div class="max-w-3xl mx-auto p-8 bg-white shadow-xl rounded-lg border border-gray-200">
        <h1 class="text-3xl font-bold text-gray-800 mb-8 text-center">🖊️ Modifier une Transaction</h1>

        <form method="post" action="{{ path('app_argent_edit', {'id': argent.id}) }}" class="space-y-6">
            {{ form_start(form) }}

            <div>
                <label class="block text-gray-700 font-semibold mb-2">Revenus (€)</label>
                {{ form_widget(form.revenus, {'attr': {'class': 'w-full p-3 border border-gray-300 rounded-lg focus:ring-2 focus:ring-green-500'}}) }}
            </div>

            <div>
                <label class="block text-gray-700 font-semibold mb-2">Dépenses (€)</label>
                {{ form_widget(form.depenses, {'attr': {'class': 'w-full p-3 border border-gray-300 rounded-lg focus:ring-2 focus:ring-red-500'}}) }}
            </div>

            <div>
                <label class="block text-gray-700 font-semibold mb-2">Solde (€)</label>
                {{ form_widget(form.solde, {'attr': {'class': 'w-full p-3 border border-gray-300 rounded-lg focus:ring-2 focus:ring-blue-500'}}) }}
            </div>

            <div>
                <label class="block text-gray-700 font-semibold mb-2">Date</label>
                {{ form_widget(form.date, {'attr': {'class': 'w-full p-3 border border-gray-300 rounded-lg focus:ring-2 focus:ring-gray-500'}}) }}
            </div>

            <div>
                <label class="block text-gray-700 font-semibold mb-2">Description</label>
                {{ form_widget(form.description, {'attr': {'class': 'w-full p-3 border border-gray-300 rounded-lg focus:ring-2 focus:ring-gray-500'}}) }}
            </div>

            <button type="submit"
                    class="w-full px-6 py-3 bg-[#06D6A0] hover:bg-[#05c093] text-white font-bold rounded-full shadow-lg transition duration-300">
                💾 Mettre à jour
            </button>

            {{ form_end(form) }}
        </form>

        <div class="mt-8 flex justify-between">
            <a href="{{ path('app_argent_index') }}"
               class="px-6 py-3 bg-[#1f1c2c] hover:bg-[#171522] text-white font-bold rounded-full shadow-md transition duration-300">
                ← Retour à la liste
            </a>

            {{ include('argent/_delete_form.html.twig') }}
        </div>
    </div>
{% endblock %}
